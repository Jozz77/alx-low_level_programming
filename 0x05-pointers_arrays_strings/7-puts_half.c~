#include "main.h"
#include <stdio.h>

/**
* puts2 - prints every other character in string.
*
* @str: the string
*
* Return: the output string.
*/

void puts2(char *str)
{
int j = 0;
while (str[j] != '\0')
{
if (j % 2 == 0)
{
putchar(str[j]);
}
j++;
}
putchar('\n');
}
