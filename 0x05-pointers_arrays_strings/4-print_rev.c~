#include "main.h"
#include <stdio.h>

/**
* _puts - prints a string.
*
* @str: the string
*
* Return: the given string
*/

void print_rev(char *s)
{
  int idx;
while (s[idx])
  idx++;
 while (idx++)
   {
     putchar(s[idx]);
       }
putchar('\n');
}
