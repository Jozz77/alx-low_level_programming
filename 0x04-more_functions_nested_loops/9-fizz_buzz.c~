#include <stdio.h>
#include "main.h"

int main(void)
{
  int i;
  for (i = 0; i <= 1000; i++)
    {
      if ((i % 3) == 0 && (i % 5) == 0)
	printf("FizzBuzz");

      else if ((i % 3) == 0)
	prinf("Fizz");
      else if ((i % 5) == 0)
	printf("Buzz");
      else
	printf("%d", i);
      if (i == 100)
	continue;
      printf(" ");

      }
      printf("\n");
      
      return (0);
}
