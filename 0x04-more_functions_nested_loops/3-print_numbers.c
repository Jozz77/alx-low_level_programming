#include <stdio.h>

/**
* print_numbers - prints the numners 0 to 9.
*
* Return: The sequence in order from 0 to 0.
*/

void print_numbers(void)
{
int i;
for (i = '0'; i <= '9'; i++)
putchar(i);
putchar('\n');
}
