#include <stdio.h>

/**
* print_most_numbers - Excluding 2 and 4, prints the numners 0 to 9.
*
* Return: The sequence in order from 0 to 9 expect 2 and 4.
*/

void print_most_numbers(void)
{
int i;
for (i = '0'; i <= '9'; i++)
{
if (i != '2' && i != '4')
putchar(i);
}
putchar('\n');
}
