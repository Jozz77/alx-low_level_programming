#include <stdio.h>

/**
* mul - mutiplies two integers.
* @a: Integer a needs to e multiplied.
* @b: The second integer to be multiplied.
* Return: the value of a multiplied by b.
*/

int mul(int a, int b)
{
return (a * b);
}
