#include "main.h"
#include <stdio.h>

/**
 *factorial - prints the factorial of a given number
 *@n: the factorial
 *
 * Return: the given factorial
 */

int factorial(int n)
{
	if (n < 0)
		return (-1);
	else if (n == 0 || n == 1)
		return (n);
	else
		return (n * factorial(n - 1));
}
