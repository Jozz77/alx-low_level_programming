#include "main.h"
#include <stdio.h>

/**
 *_strlen_recursion - prints the lenght of thr string
 *@s: the string
 *
 * Return: the lenght
 */

int _strlen_recursion(char *s)
{
	if (*s == '\0')
		return (0);

	s++;
	return (1 + _strlen_recursion(s));
}
