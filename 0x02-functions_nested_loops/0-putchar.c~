#include <stdio.h>
/**
* main - Entry point
*
* Description: 'a program that prints _putchar, followed by a new line'
*
* Return: Always 0 (Success)
*/

int main()
{
  char ch = "utchar";

  putchar(ch);

  return (0);
}
